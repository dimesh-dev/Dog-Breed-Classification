streamlit==1.38.0
# Use a CPU-only TensorFlow build. Adjust the version if you need newer TF features.
# On Windows the package name is the same (tensorflow); on Linux many hosts support this too.
tensorflow==2.12.0
numpy
